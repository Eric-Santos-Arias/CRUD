
# Conexión a psql database                             #nombre de la base de datos 
spring.datasource.url=jdbc:postgresql://localhost:5432/escuela 
spring.datasource.username=eric
spring.datasource.password=123
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
#esto lo saque de stack overflow para mejora de rendimiento aun no lo entiendo del todo

#donde va a correr el servidor mantener puerto 3000 para que no choque con el frontend corriendo en 8080
server.port = 3000
