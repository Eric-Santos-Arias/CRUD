# 📝 CRUD Básico con Express


---
Este proyecto implementa un **CRUD** (Create, Read, Update, Delete), las operaciones esenciales para manejar datos en cualquier aplicación.  
Se utiliza **Express.js para el backend a si como **HTML y JavaScript** para fronted para la base de Datos se usa **PostgreSql** se agrego un backend hecho con **Java**


---
**Se agregará** más adelante un backend implementando un  **CRUD** hecho con **C#** manteniendo la misma funcionalidad sin modificar el frontend.
en la carpeta analisis se encuentra un breve analisis sobre que tecnologia se podría estar utilizando.
